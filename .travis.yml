language: php

php:
  - 7.0
  - 7.1

sudo: false

install:
  - composer self-update
  - travis_retry composer install --no-interaction --prefer-source

script: vendor/bin/phpunit --configuration plugins/integration/api/phpunit.xml